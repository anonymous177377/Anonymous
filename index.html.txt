<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Painel Hacker</title>
<style>
:root{--green:#00ff00;--red:#ff0000;--bg:#000}
*{box-sizing:border-box}
html,body{height:100%;margin:0;background:var(--bg);font-family:monospace;color:var(--green);overflow:hidden}

/* MATRIX */
canvas{position:fixed;left:0;top:0;width:100%;height:100%;z-index:0}

/* Login screen */
#login-screen{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;z-index:10;background:rgba(0,0,0,0.5);animation:redAlert 1s infinite}
#login-screen h1{margin:0 0 12px 0;font-size:34px;animation:intenseGlow 2s infinite;color:var(--red)}
#login-screen input{background:#000;border:1px solid var(--green);color:var(--green);padding:10px;margin:6px;width:300px;outline:none}
#login-screen button{background:#000;border:1px solid var(--green);color:var(--green);padding:10px 20px;cursor:pointer}

/* panel */
#panel{display:none;position:relative;z-index:5;text-align:center;padding-top:50px;opacity:0;transition:opacity 1s ease,transform 0.25s ease}
button.menu-btn{background:#000;border:1px solid var(--green);color:var(--green);padding:12px 24px;margin:12px;cursor:pointer}

/* aggressive theme */
@keyframes redAlert{0%,100%{background-color:rgba(0,0,0,0.9)}50%{background-color:rgba(120,0,0,0.6)}}
@keyframes intenseGlow{0%{text-shadow:0 0 5px var(--red)}50%{text-shadow:0 0 25px var(--red)}100%{text-shadow:0 0 5px var(--red)}}
@keyframes shake{0%{transform:translate(0)}20%{transform:translate(-5px,2px)}40%{transform:translate(3px,-3px)}60%{transform:translate(-3px,4px)}80%{transform:translate(4px,-2px)}100%{transform:translate(0)}}
@keyframes glitch{0%{clip-path:inset(0 0 0 0)}20%{clip-path:inset(10% 0 50% 0)}40%{clip-path:inset(30% 0 20% 0)}60%{clip-path:inset(5% 0 40% 0)}80%{clip-path:inset(25% 0 15% 0)}100%{clip-path:inset(0 0 0 0)}}

.glitch-text{animation:glitch .7s infinite;position:relative}
.system-failure{color:var(--red);font-size:20px;margin:18px;animation:shake .4s infinite}

/* small screens */
@media (max-width:480px){
  #login-screen input{width:200px}
  #login-screen h1{font-size:22px}
  .system-failure{font-size:16px}
}
</style>
</head>
<body>

<canvas id="matrix"></canvas>

<div id="login-screen">
  <h1>ACESSO RESTRITO</h1>
  <input id="user" type="text" placeholder="Login" value="">
  <input id="pass" type="password" placeholder="Senha" value="">
  <button id="enterBtn">ENTRAR</button>
</div>

<div id="panel" aria-hidden="true">
  <div class="system-failure glitch-text">⚠ FALHA CRÍTICA NO SISTEMA — ACESSO NÃO AUTORIZADO DETECTADO ⚠</div>
  <h1>PAINEL HACKER</h1>
  <button class="menu-btn" onclick="window.location.href='https://www.vexpainel.com/consultar/'">Abrir Consulta</button>
  <button class="menu-btn">Opção 2</button>
  <button class="menu-btn">Opção 3</button>
</div>

<!-- Sons: usei URLs públicos para tocar automaticamente no login -->
<audio id="startup-sound" src="https://actions.google.com/sounds/v1/alarms/digital_watch_alarm_long.ogg"></audio>
<audio id="alarm" src="https://actions.google.com/sounds/v1/alarms/alarm_clock.ogg" loop></audio>

<script>
/* ---------- MATRIX ---------- */
const canvas = document.getElementById('matrix');
const ctx = canvas.getContext('2d');
let fontSize = 16;
let cols = Math.floor(window.innerWidth / fontSize);
let drops = Array.from({length: cols}, ()=>1);

function resizeCanvas(){
  canvas.width = innerWidth;
  canvas.height = innerHeight;
  cols = Math.floor(canvas.width / fontSize);
  drops = Array.from({length: cols}, (v,i)=>drops[i] || 1);
}
window.addEventListener('resize', resizeCanvas);
resizeCanvas();

function drawMatrix(){
  ctx.fillStyle = "rgba(0,0,0,0.06)";
  ctx.fillRect(0,0,canvas.width,canvas.height);
  ctx.fillStyle = "#00ff00";
  ctx.font = fontSize + "px monospace";
  const letters = "ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
  for(let i=0;i<drops.length;i++){
    const text = letters[Math.floor(Math.random()*letters.length)];
    ctx.fillText(text, i*fontSize, drops[i]*fontSize);
    if(drops[i]*fontSize > canvas.height && Math.random()>0.975) drops[i]=0;
    drops[i]++;
  }
}
setInterval(drawMatrix, 33);

/* ---------- LOGIN + TRANSITIONS + SONS + AGGRESSIVE EFFECT ---------- */
const correctUser = "RickAlves76";
const correctPass = "04052023";
const loginScreen = document.getElementById('login-screen');
const panel = document.getElementById('panel');
const startup = document.getElementById('startup-sound');
const alarm = document.getElementById('alarm');
const enterBtn = document.getElementById('enterBtn');

enterBtn.addEventListener('click', login);

function login(){
  const u = document.getElementById('user').value.trim();
  const p = document.getElementById('pass').value.trim();
  if(u === correctUser && p === correctPass){
    // tentar tocar os sons (browsers podem bloquear autoplay)
    try{ startup.play(); }catch(e){}
    try{ alarm.play(); }catch(e){}
    // transição suave
    loginScreen.style.transition = 'opacity 1s ease, transform 0.3s ease';
    panel.style.transition = 'opacity 1s ease';
    loginScreen.style.opacity = 0;
    loginScreen.style.transform = 'scale(.995)';
    setTimeout(()=>{
      loginScreen.style.display = 'none';
      panel.style.display = 'block';
      setTimeout(()=>{ panel.style.opacity = 1; panel.removeAttribute('aria-hidden'); }, 50);
      aggressiveSequence();
    },900);
  } else {
    alert('Acesso negado!');
  }
}

function aggressiveSequence(){
  // blackouts + extra screen shake
  let flashes = 0;
  const iv = setInterval(()=>{
    document.body.style.background = (flashes % 2 == 0) ? '#300' : '';
    flashes++;
    if(flashes>6){ clearInterval(iv); document.body.style.background = ''; }
  }, 180);

  let shakes = 0;
  const sh = setInterval(()=>{
    panel.style.transform = 'translate('+ (Math.random()*8-4) +'px,'+ (Math.random()*8-4) +'px)';
    shakes++;
    if(shakes>10){ clearInterval(sh); panel.style.transform='none'; }
  }, 60);
}

window.addEventListener('beforeunload', ()=>{ try{ alarm.pause(); alarm.currentTime = 0;}catch(e){} });
</script>
</body>
</html>